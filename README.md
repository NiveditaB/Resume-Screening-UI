# Resume-Screening-UI
A Resume Screening Web App that uses TF-IDF, Regex-based text extraction, and a lightweight LLM pipeline to analyze resumes, identify key skills, compute similarity scores, and generate structured screening results with an interactive UI.
